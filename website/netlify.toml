[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Let Netlify Forms handle POST submissions directly (bypass Nuxt function)
[[redirects]]
  from = "/netlify-forms.html"
  to = "/netlify-forms.html"
  status = 200
  force = false
